import { AuthLayout } from "../shared"
import { LoginForm } from "./components"

export const Login = () => {
    return (
        <AuthLayout>
            <LoginForm />
        </AuthLayout>
    )
}