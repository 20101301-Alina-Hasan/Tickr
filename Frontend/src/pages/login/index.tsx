import { AuthLayout } from "@/components/common"
import { LoginForm } from "./components"

export const Login = () => {
    return (
        <AuthLayout>
            <LoginForm />
        </AuthLayout>
    )
}